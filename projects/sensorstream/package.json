{
  "name": "sensorstream",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Real-time IoT sensor data pipeline — streaming, validation, cleaning, storage, and live dashboard",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "express": "^4.21.0",
    "helmet": "^8.0.0",
    "zod": "^3.23.0"
  }
}
