/* ─── Spectra Glass Site — Entry Point ───
   Importing the library registers all custom elements.
   Individual component imports are only needed if you
   want tree‑shaking. */

import 'spectra-glass-ui';
