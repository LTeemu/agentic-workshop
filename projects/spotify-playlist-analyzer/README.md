# Spotify Playlist Analyzer

Analyze your Spotify playlist export CSV — **Python** for data analysis, **Flask** for the dashboard.

Record label aesthetic with warm blacks, browns, and gold.

## Quick Start

### Prerequisites

- Python 3.10+ with `pip`

### 1. Drop your CSV

Place your Spotify playlist CSV in `data/spotify-playlist.csv`.  
If no CSV is found, the server falls back to `data/spotify-playlist-example.csv` (a sample dataset for testing).

The CSV can be tab-separated or comma-separated (auto-detected). Column names are matched case-insensitively.

| Column                                               | Description                     |
| ---------------------------------------------------- | ------------------------------- |
| Song                                                 | Track name                      |
| Artist                                               | Artist name(s), comma-separated |
| BPM                                                  | Beats per minute                |
| Camelot                                              | Camelot wheel notation          |
| Energy                                               | 0–100                           |
| Added at                                             | Date added to playlist          |
| Duration                                             | mm:ss                           |
| Popularity                                           | 0–100                           |
| Genres                                               | Comma-separated tags            |
| Album                                                | Album name                      |
| Album Date                                           | Release date                    |
| Dance, Acoustic, Instrumental, Valence, Speech, Live | 0–100                           |
| Loud (db)                                            | Decibels                        |
| Key                                                  | Musical key                     |
| Time Signature                                       | Time signature                  |
| Spotify Track Id                                     | URI                             |
| ISRC                                                 | Recording code                  |
| Explicit                                             | yes / no                        |

### 2. Install & run

```bash
cd python
pip install -r requirements.txt
python server.py
```

The server will:

1. Check if `analysis.json` is missing or older than your CSV
2. Auto-run `analyze.py` if needed
3. Start the dashboard at **http://localhost:5000**

Or from the project root:

```bash
npm start
```

### Manual commands

```bash
cd python
python analyze.py ../data/spotify-playlist.csv -o ../data/analysis.json
```

**Dev iteration** — process only first N rows for faster testing:

```bash
python analyze.py ../data/spotify-playlist.csv -o ../data/analysis.json --limit 10
```

### Iteration

- **Edit code** → Flask reloads automatically in debug mode
- **Update CSV** → restart the server (it auto-rebuilds on boot)
- **Re-run analysis** → page refresh picks up new data (mtime cache)
- All `avg_*` stats use **median** (not mean) for robustness
- Popularity=0 is treated as missing data (696 of 3816 tracks)

## Project Structure

```
├── data/
│   ├── spotify-playlist.csv              ← your playlist CSV export (primary)
│   ├── spotify-playlist-example.csv       ← 18-song sample for testing (fallback)
│   └── analysis.json           ← generated by analyze.py
├── python/
│   ├── requirements.txt        ← pandas, numpy, flask
│   ├── analyze.py              ← data analysis engine
│   ├── server.py               ← Flask dashboard server
│   ├── templates/              ← Jinja2 templates (8 pages)
│   │   ├── layout.html
│   │   ├── index.html          (overview)
│   │   ├── audio.html          (audio features)
│   │   ├── genres.html         (genre breakdown)
│   │   ├── artists.html        (top artists)
│   │   ├── mood.html           (key + mood quadrants)
│   │   ├── timeline.html       (temporal analysis)
│   │   ├── 404.html
│   │   └── error.html
│   └── static/
│       └── style.css           ← record label aesthetic
├── package.json                ← workshop integration (npm start)
└── README.md
```

## How It Works

```
spotify-playlist.csv
        │
        ▼  (python/analyze.py)
  data/analysis.json          ← full structured analysis
        │
        ▼  (python/server.py reads JSON, caches by mtime)
  Flask @ localhost:5000
        │
        ▼  (Chart.js in browser)
  Interactive dashboard
```

The Python analyzer computes seven analysis categories and writes a single JSON file. The Flask server reads that file (with mtime-based caching — re-reads only when the file changes) and renders Chart.js visualizations with per-bar HSL colors.

## Analysis Categories

| Category           | What it includes                                                                                                                                                                                                        |
| ------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Basic Stats**    | Total songs, unique artists/genres/albums, median BPM/energy/dance/valence/duration/popularity, explicit count                                                                                                          |
| **Audio Features** | Bucketed histograms for BPM, Energy, Dance, Valence, Acousticness, Instrumentalness, Speechiness, Liveness, Loudness                                                                                                    |
| **Genres**         | Top 20 genres with counts and percentages, total unique genres                                                                                                                                                          |
| **Artists**        | Top 20 artists, collaboration count, per-artist genre tags                                                                                                                                                              |
| **Mood & Key**     | Key distribution, Camelot wheel, mood quadrants (valence × energy), interactive scatter plot                                                                                                                            |
| **Timeline**       | Songs added by year/month, album release year distribution                                                                                                                                                              |
| **Perfect Track**  | Mode (most common) value for each audio feature, composite attributes (key, camelot, genre, time signature), closest real match (most representative song) and furthest real match (biggest outlier) from your playlist |

## Dashboard Pages

| Route         | Page                                                     |
| ------------- | -------------------------------------------------------- |
| `/`           | Overview — stat cards, vinyl hero, perfect track profile |
| `/audio`      | Audio feature histograms                                 |
| `/genres`     | Genre breakdown                                          |
| `/artists`    | Top artists                                              |
| `/mood`       | Key + mood analysis                                      |
| `/timeline`   | Temporal analysis                                        |
| `/api/health` | Health check for workshop integration                    |

## Workshop Integration

The project supports the agentic-workshop dashboard at `http://localhost:3000`:

- `package.json` with `"start": "cd python && python server.py"` script
- Server respects `PORT` environment variable (default: 5000)
- `/api/health` returns `{"status": "ok"}`
